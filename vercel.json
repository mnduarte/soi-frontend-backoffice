{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "SPA fallback so deep links (e.g. /accounts) don't 404 on refresh. Static files keep being served directly; anything that doesn't match falls back to index.html where React Router takes over.",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
